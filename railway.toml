[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python -m pip install -r requirements.txt && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
